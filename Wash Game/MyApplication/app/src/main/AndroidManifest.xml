<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
    <!-- this is defined in org.opendatakit.services -->
    <uses-permission android:name="org.opendatakit.sync.RUN_SYNC" /> <!-- this is defined in org.opendatakit.services -->
    <uses-permission android:name="org.opendatakit.webkitserver.RUN_WEBSERVER" /> <!-- this is defined in org.opendatakit.services -->
    <uses-permission android:name="org.opendatakit.database.RUN_DATABASE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Opendatakit">

        <activity android:name=".ResultActivity"/>
        <activity android:name=".GameActivity" />
        <activity android:name=".SelectGameActivity" />
        <activity android:name=".CreateFormActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>